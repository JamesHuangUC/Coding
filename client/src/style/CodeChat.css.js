const container = {
    display: "flex",
    // minHeight: 500,
    height: "75vh"
};

const leftPanel = {
    display: "flex",
    flexDirection: "column",
    flex: 4
};

const rightPanel = {
    flex: 1
};

export default { container, leftPanel, rightPanel };
